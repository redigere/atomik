---

- name: Enable App Nap service
  ansible.builtin.systemd:
    name: atomik-appnap.service
    state: started
    enabled: true
    daemon_reload: true
    scope: user
  become: true
  become_user: "{{ atomik_user }}"
